import { StatusMessage } from '../components/StatusMessage'

export function TropelsPage() {
  return (
    <StatusMessage
      title="Atlas de Tropeles"
      text="Siguiente bloque: paginacion real, filtros combinables, busqueda, ordenamiento y estado en URL."
    />
  )
}
