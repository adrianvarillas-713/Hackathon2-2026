import { StatusMessage } from '../components/StatusMessage'

export function StoryPage() {
  return (
    <StatusMessage
      title="Sector Story Engine"
      text="Bloque hard: scrollytelling por etapas, progreso, fallback, reduced motion y teclado."
    />
  )
}
