import { StatusMessage } from '../components/StatusMessage'

export function SignalDetailPage() {
  return (
    <StatusMessage
      title="Detalle de Senal"
      text="Siguiente bloque: detalle real, cambio de estado, loading, error y confirmacion."
    />
  )
}
