import { StatusMessage } from '../components/StatusMessage'

export function SignalsPage() {
  return (
    <StatusMessage
      title="Feed de Senales"
      text="Siguiente bloque: infinite scroll con cursor, deduplicacion y filtros persistidos en URL."
    />
  )
}
